apiVersion: zalando.org/v1
kind: FabricGateway
metadata:
  name: testOperatorName-stack-managed
spec:
  x-external-service-provider:
    stackSetName: gw-test-stack
    hosts:
      - ss-testOperatorName.{{ CLUSTER }}.zalan.do
  x-fabric-admins:
  - bmooney
  - fmoloney
  - cgallagher
  - changedForTesting
  paths:
    /resources:
      get: {}