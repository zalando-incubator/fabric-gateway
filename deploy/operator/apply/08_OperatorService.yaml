apiVersion: v1
kind: Service
metadata:
  name: fgtestapp-operator
spec:
  selector:
    application: fgtestapp-operator
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  - name: https
    port: 443
    protocol: TCP
    targetPort: 8443